// select the header element
let headerElement = document.querySelector('header'); 

// change the color of the header element on click
red_header.addEventListener('click', () => {
    headerElement.style.color = '#FF0000';
    });
